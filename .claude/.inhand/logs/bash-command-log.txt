cd /Users/cassiandrefke/Developer/TDC-Advisory
export COREPACK_ENABLE_DOWNLOAD_PROMPT=0
S="/private/tmp/claude-501/-Users-cassiandrefke-Developer-TDC-Advisory/a4f13fd3-9177-4846-a55b-b3de49e524f3/scratchpad"
mkdir -p "$S"
echo "=== can this build as plain Next.js? ==="
corepack pnpm exec next build > "$S/nextbuild.log" 2>&1
echo "rc=$?"
tail -30 "$S/nextbuild.log" - Test building as plain Next.js
cd /Users/cassiandrefke/Developer/TDC-Advisory
S="/private/tmp/claude-501/-Users-cassiandrefke-Developer-TDC-Advisory/a4f13fd3-9177-4846-a55b-b3de49e524f3/scratchpad"
export COREPACK_ENABLE_DOWNLOAD_PROMPT=0
pkill -f "run-framework.mjs" 2>/dev/null; pkill -f vinext 2>/dev/null; pkill -f "next dev" 2>/dev/null; sleep 2
nohup corepack pnpm exec next start -p 3100 > "$S/nextstart.log" 2>&1 & disown
for i in $(seq 1 25); do
  c=$(curl -s -o /dev/null -w '%{http_code}' --max-time 8 http://localhost:3100/ 2>/dev/null)
  [ "$c" = "200" ] && { echo "next start ready after $((i*2))s"; break; }; sleep 2
done
echo "=== routes under real Next.js ==="
for p in / /imprint /sitemap.xml /robots.txt /llms.txt /og-image.png; do
  printf '  %-14s ' "$p"; curl -s -o /dev/null -w 'http=%{http_code} %{content_type}\n' --max-time 30 "http://localhost:3100$p"
done
echo "=== SEO head intact? ==="
curl -s --max-time 30 http://localhost:3100/ | grep -oE '<title>[^<]*</title>|rel="canonical"|og:image|application/ld\+json' | sort -u | sed 's/^/  /' - Run the Next.js production build locally
cd /Users/cassiandrefke/Developer/TDC-Advisory
echo "=== .next ignored by git? ==="; grep -n '\.next' .gitignore | sed 's/^/  /'
node -e "
const fs=require('fs'); const p='package.json';
const j=JSON.parse(fs.readFileSync(p,'utf8'));
const before={...j.scripts};
// Vercel detects Next.js and expects .next/. Build with Next so local and
// production use one toolchain instead of diverging.
j.scripts.dev='next dev';
j.scripts.build='next build';
j.scripts.start='next start';
fs.writeFileSync(p, JSON.stringify(j,null,2)+'\n');
console.log('scripts:');
for (const k of ['dev','build','start']) console.log('  '+k+':  '+before[k]+'  ->  '+j.scripts[k]);
" - Switch build scripts to Next.js
cd /Users/cassiandrefke/Developer/TDC-Advisory
S="/private/tmp/claude-501/-Users-cassiandrefke-Developer-TDC-Advisory/a4f13fd3-9177-4846-a55b-b3de49e524f3/scratchpad"
export COREPACK_ENABLE_DOWNLOAD_PROMPT=0
pkill -f "next start" 2>/dev/null; sleep 2
echo "=== pnpm build (as Vercel will run it) ==="
corepack pnpm build > "$S/verify-build.log" 2>&1; echo "  rc=$?"
sed -n '/Route (app)/,/prerendered/p' "$S/verify-build.log" | sed 's/^/  /'
echo
echo "=== the artifact Vercel was looking for ==="
ls -l .next/routes-manifest.json 2>/dev/null | awk '{print "  "$5" bytes  "$9}' || echo "  MISSING"
echo
echo "=== pnpm dev works too? ==="
nohup corepack pnpm dev -p 3101 > "$S/verify-dev.log" 2>&1 & disown
for i in $(seq 1 25); do
  c=$(curl -s -o /dev/null -w '%{http_code}' --max-time 8 http://localhost:3101/ 2>/dev/null)
  [ "$c" = "200" ] && { echo "  dev ready after $((i*2))s"; break; }; sleep 2
done
for p in / /imprint /sitemap.xml /robots.txt; do printf '  %-14s ' "$p"; curl -s -o /dev/null -w 'http=%{http_code}\n' --max-time 30 "http://localhost:3101$p"; done - Verify build and dev under Next scripts
cd /Users/cassiandrefke/Developer/TDC-Advisory
node - <<'EOF'
import { readFileSync, writeFileSync } from 'node:fs';
const p = 'README.md';
let s = readFileSync(p, 'utf8');

s = s.replace(
  'React 19 + TypeScript + Tailwind CSS 4, built with vinext (Next-compatible\napp router on Vite) and served from a Cloudflare Worker.',
  'Next.js 16 (app router) + React 19 + TypeScript + Tailwind CSS 4.\nEvery route prerenders as static content.'
);

s = s.replace(
  `\`\`\`sh
pnpm build   # emits dist/ (client assets + Worker bundle)
pnpm start   # serves dist/ locally through Wrangler
\`\`\``,
  `\`\`\`sh
pnpm build   # emits .next/
pnpm start   # serves the production build locally
\`\`\``
);

s = s.replace(
  `## Deployment

The build targets the Cloudflare Workers runtime. \`pnpm build\` writes the Worker
bundle and its configuration to \`dist/\`, ready for \`wrangler deploy\` or a
Cloudflare Workers Git integration.

Generated \`dist/\`, \`.wrangler/\` and \`.sites-runtime/\` are disposable and ignored
by Git.`,
  `## Deployment

Deployed on Vercel from \`main\`. Vercel detects Next.js and runs \`pnpm build\`;
no additional configuration is required.

All routes are static, so the site is served from the edge cache.

Generated \`.next/\`, \`dist/\`, \`.wrangler/\` and \`.sites-runtime/\` are disposable
and ignored by Git.`
);
writeFileSync(p, s);
console.log('README deployment section updated');
EOF
grep -n 'Cloudflare\|Worker\|wrangler' README.md | sed 's/^/  /' || echo "  no stale Cloudflare references in README" - Update README for Vercel deployment
cd /Users/cassiandrefke/Developer/TDC-Advisory
git add -A
git diff --cached --stat | tail -5 | sed 's/^/  /'
git commit -q -F - <<'EOF'
fix: build with Next.js so Vercel can deploy

Vercel detects Next.js from the dependency and expects .next/, but the build
script ran vinext, which emits a Cloudflare Worker bundle in dist/. The deploy
failed looking for .next/routes-manifest.json.

The app was already standard Next.js — app router, metadata API, sitemap.ts
and robots.ts, with no vinext or Cloudflare imports in app/, components/ or
lib/ — so it builds directly with `next build`. Every route prerenders as
static content, including /sitemap.xml and /robots.txt.

- dev, build and start now use Next, so local matches production. The previous
  split risked divergence; next/link already behaved differently under vinext.
- README deployment section updated for Vercel.

The vinext, Vite, Wrangler and Cloudflare dependencies are now unused but left
in place; removing them is a separate change.

Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>
EOF
git log --oneline -1 | sed 's/^/  /'
echo "=== pushing ==="
git push 2>&1 | tail -3 | sed 's/^/  /' - Commit and push the Vercel fix
